//entre los parentesis se ponen LOS PARÁMETROS DE LA FUNCIÓN.
// entre las llaves se pone EL CUERPO DE LA FUNCIÓN.

//El símbolo ' + ' se usa para concatenar.
function saludar(nombre) { 
    return "Hola" + nombre
}

//Crear otras variable:

let mi_nombre = "Juan Tabares"

// para llamar una función se hace así:
//Entre los parentesis se pone lo que se espera que haga la función 
let resultado = saludar(mi_nombre)

//Aquí esperamos compilar de .ts(TypeScript) a .js(Javascript):
//Se abre la terminal 